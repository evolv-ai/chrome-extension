chrome.runtime.onInstalled.addListener(() => {
 
    // TODO do I need to clear storage somehow?
    // chrome.storage.local.clear();
    // chrome.storage.managed.clear();
    // chrome.storage.sync.clear();

});